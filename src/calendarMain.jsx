import React from 'react'
import ReactDOM from 'react-dom/client'
import ExecutiveCalendarPage from './ExecutiveCalendarPage.jsx'
import '../index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <ExecutiveCalendarPage />
    </React.StrictMode>
)
